apiVersion: batch/v1
kind: Job
metadata:
  name: errjob-unvalidcommand
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: errjob-unvalidcommand
          image: busybox
          command: ["ls", "unvalid path"]
